import 'package:flutter/material.dart';
import 'package:company/rate.dart';

class GiPipeRate extends StatelessWidget {
  const GiPipeRate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body:Rate(heading: 'GI PIPE',Length:5),

    );
  }
}
